# HE Manager 前端优化专项计划 (FRONTEND_PLAN.md)

> 目标：系统提升 Web 前端的视觉观感（UI 质感、动效、排版层级、响应式）与操作逻辑（UX 链路、快捷键、交互反馈、效率工具），做一步勾一步，闭环交付。

---

## 执行规则

1. **按任务模块分阶段推进**：每个任务聚焦明确的组件与交互范围，一次推进一个清晰任务。
2. **跨平台兼容与构建保障**：每次修改后运行 `npm run build`（包含 `vue-tsc` 类型检查）与 `npm test`，确保无报错、无隐式类型断裂。
3. **保护 Vue 3 单根结构**：各路由视图顶层保持单根元素，避免在 `<template>` 根层产生注释 VNode 导致 `<transition>` 空白页。
4. **即时验收与状态更新**：每项完成后把 `[ ]` 改为 `[x]`，并在表格/清单中注明改动成果。

---

## 优化任务总览

| # | 任务模块 | 核心优化目标 | 状态 | 成果记录 |
|---|---|---|:---:|---|
| **1** | 全局布局与侧边栏 (`App.vue`, `Sidebar.vue`) | Mini 图标折叠栏、导航语义分组、全局快捷键、返回顶部按钮 | [x] | 桌面端 Mini 模式、语义三组导航、Tooltip、平滑返回顶部已上线并通过构建与测试 |
| **2** | 媒体库主页与卡片流 (`HomeView.vue`, `MediaCard.vue`) | 双行标题截断、搜索一键清除与标签过滤搜索、继续观看栏临时隐藏/折叠、微型评分徽章 | [x] | 卡片两行防截断、评分金星、搜索X一键清除、标签微型过滤、继续栏折叠与移除已闭环并通过构建与测试 |
| **3** | 媒体详情与元数据面板 (`MediaDetail.vue`, `MetadataPanel.vue`) | 元数据侧栏独立收起/展开、打标签自动补全联想、星级评分实时 Hover 动效、键盘输入防冲突 | [ ] | 待开始 |
| **4** | 漫画阅读器体验升级 (`MangaReader.vue`) | 阅读模式切换（单页 / 双页跨页拼合 / 垂直长图连续卷轴）、快速定位滑块、热键引导气泡 | [ ] | 待开始 |
| **5** | 单图查看器体验打磨 (`ImageViewer.vue`) | 图片加载中骨架/转圈、顺逆时针 90° 旋转、1:1 原图尺寸切换、动图控制指示 | [ ] | 待开始 |
| **6** | 音频播放器全栈重构 (`AudioPlayer.vue`) | 摆脱原生灰色 audio 控件（自建定制玻璃态播放条）、倍速切换、简单歌词预览、页面底部常驻 Mini Bar | [ ] | 待开始 |
| **7** | 视频播放与快捷手势 (`VideoPlayer.vue`, `MediaDetail.vue`) | 视频长按快进/快退 HUD 视觉动效升级、快捷键防误触、全屏与宽屏切换优化 | [ ] | 待开始 |
| **8** | 整理与发现页面 (`TagsView`, `CreatorsView`, `DedupView`) | 统一全站分页组件接入、标签直达媒体库与0引用清理、创作者首字母检索、重复管理优劣属性红绿对比与即时预览 | [ ] | 待开始 |
| **9** | 系统设置、外部工具与推荐 (`SettingsView`, `ExternalFavoritesView`, `MangaRecommendView`, `StatsView`) | 外部收藏 Segmented Tabs 扁平化、偏好设置紧凑化与快捷键指南面板、推荐抽屉 Click-outside 关闭、统计面板顶部快速定位锚点 | [ ] | 待开始 |

---

## 详细任务清单与验收标准

### 任务 1：全局布局与侧边栏交互 (`App.vue`, `Sidebar.vue`)
- [x] **1.1 侧边栏支持 Mini 紧凑图标模式**
  - **现状**：桌面端折叠后直接变成 `w-0` 完全隐藏，切换页面需先点开侧边栏。
  - **改进**：桌面端折叠时变为 `w-16` Mini 窄栏，仅显示精简图标；Hover 时弹出优雅的黑色半透明 Tooltip 气泡提示菜单名；移动端窄屏保留抽屉覆盖。
- [x] **1.2 导航项语义层级分组**
  - **现状**：13 项菜单平铺一列，长纵向偏挤，寻找功能认知成本高。
  - **改进**：划分为三组并添加微小标题/分隔：
    1. **媒体库**（全部、视频、漫画、杂图、音频、收藏）
    2. **发现与整理**（外部收藏、AI 推荐、创作者、标签管理）
    3. **系统与工具**（BD2 动态、统计、重复管理）
- [x] **1.3 刷新与底部状态反馈优化**
  - 侧边栏刷新按钮和折叠按钮的 Hover 交互与旋转动效微调，退出登录增加防误触提示。
- [x] **1.4 悬浮返回顶部按钮 (Back to Top)**
  - 在主屏大幅向下滚动（如超过 400px）时，右下角平滑浮现返回顶部毛玻璃按钮，点击顺滑滚回顶部。

---

### 任务 2：媒体库主页与卡片流 (`HomeView.vue`, `MediaCard.vue`)
- [x] **2.1 MediaCard 标题升级为双行智能截断 (`line-clamp-2`)**
  - **现状**：单行截断导致 `[作者] 作品名 卷数` 只能看到前几个字。
  - **改进**：文字区采用固定两行高度容器，长标题展示更充分，中英排版呼吸感更好，卡片规整不跳动。
- [x] **2.2 卡片元数据排版与微质感**
  - 底部格式标签、时长/页数、已看 (Eye) 状态图标更醒目；对于有评分的媒体在卡片底部增加微型金色星标与分值。
  - 增强在暗黑背景下的可读性（将部分过暗的 `text-white/30` 提升至更清晰的对比度）。
- [x] **2.3 筛选器交互链路补全**
  - 搜索框内增加一键清除 (X) 按钮，点击清空并立即恢复列表；
  - 标签下拉弹窗增加顶部微型搜索框，支持在数十上百个标签中快速拼写筛选；
  - 来源分段按钮和排序分段按钮在视口收窄时优雅折行包裹。
- [x] **2.4 继续观看栏（Continue Section）操作便利化**
  - 支持单项 Hover 出现临时隐藏按钮（或长按移除最近历史）；
  - 右侧增加快速折叠/展开开关，方便专注浏览媒体海报墙。

---

### 任务 3：媒体详情弹窗与元数据面板 (`MediaDetail.vue`, `MetadataPanel.vue`)
- [x] **3.1 元数据侧栏支持一键收起/展开 (Panel Toggle)**
  - **现状**：在非全屏的浏览器窗口下，右侧固定占据 `340px`，中间媒体画面受限。
  - **改进**：在顶部或侧栏边缘增加收起把手，点击可平滑隐藏右侧元数据，媒体画面自动铺满窗口，既保留窗口边框又享有沉浸巨幕体验。
- [x] **3.2 标签输入联想补全 (Autocomplete)**
  - 添加新标签时，根据输入内容实时匹配已有标签库下拉候选，点击或按回车键直接填入，彻底解决手滑打错或标签重复分裂问题。
- [x] **3.3 星级评分 Hover 交互动效**
  - 鼠标在 1~5 颗星之间划动时，前面的星星实时点亮预览，点击完成保存，伴随轻微弹性缩放动画。
- [x] **3.4 输入焦点与快捷键防冲突**
  - 严格确保当焦点在任何输入框（Input / Textarea）时，禁用全局左右键翻页/快进，避免打字移动光标时误跳页面。
- [x] **3.5 异常状态（文件丢失）优化**
  - 重新检查按钮加入即时 Loading 状态与清晰的结果 Toast 提示。

---

### 任务 4：漫画阅读器体验深度升级 (`MangaReader.vue`)
- [x] **4.1 多阅读模式切换**
  - **单页模式 (Single)**：保持当前高精度单图居中。
  - **双页跨页模式 (Double)**：自动把相邻两页无缝拼合为跨页视图，完美还原画册与日漫跨页大图震撼感。
  - **连续卷轴模式 (Webtoon)**：垂直长图自上而下滑动模式，适合条漫浏览。
- [x] **4.2 底部控制栏与快速定位**
  - 缩略图栏与主进度条联动增加平滑拖拽定位滑块（Slider）；
  - 增加常驻/渐隐热键提示气泡（`← / → 翻页`、`0 适合屏幕`、`+/- 缩放`）。

---

### 任务 5：单图查看器体验打磨 (`ImageViewer.vue`)
- [ ] **5.1 图片加载中骨架反馈**
  - 在加载高清大图时显示居中微光转圈或骨架图，避免切图时的突然黑屏闪烁感。
- [ ] **5.2 图像 90° 旋转功能**
  - 工具栏增加旋转按钮（支持顺时针 90° 翻转），解决手机拍摄倒置或扫描倒向照片的查看问题。
- [ ] **5.3 1:1 实际像素与适应屏幕一键切换**
  - 除了现有的连续缩放，提供快捷切换“1:1 原图大小”与“适应屏幕”。

---

### 任务 6：音频播放器全栈重构 (`AudioPlayer.vue`)
- [ ] **6.1 告别原生灰白 audio 控件，定制深色现代播放器**
  - 自制一体化暗黑毛玻璃风格播放控制条（播放/暂停大按键、波形/平滑进度滑块、当前时间/总时间）。
- [ ] **6.2 倍速播放与播放模式切换**
  - 支持快捷选择播放倍速（1.0x, 1.25x, 1.5x, 2.0x）；
  - 支持单曲循环、列表顺序、随机播放模式切换。
- [ ] **6.3 歌词预览与音轨列表优化**
  - 有 LRC 歌词的音轨在播放时在上方呈现同步歌词或点击展开查看歌词文本。
- [ ] **6.4 页面底部常驻音频 Mini 播放条 (Persistent Bar)**
  - 用户在听音频或 ASMR 时关闭详情弹窗，音频不中断，自动平滑收纳至页面底部 Mini Bar；用户可以一边听一边在首页挑选新媒体。

---

### 任务 7：视频播放与快捷手势 (`VideoPlayer.vue`, `MediaDetail.vue`)
- [ ] **7.1 视频快进/快退 HUD 视觉动效升级**
  - 短按/长按快进快退时的屏幕浮层提示（HUD）居中透明胶囊展示，音量滚轮灵敏度微调。
- [ ] **7.2 视频播放模式与画中画联动**
  - 优化循环播放、顺序播放图标在移动端和不同尺寸下的可触控面积与状态提示。

---

### 任务 8：整理与管理页面交互优化 (`TagsView`, `CreatorsView`, `DedupView`)
- [ ] **8.1 统一全站分页组件接入 (`PaginationControl`)**
  - 将 `DedupView.vue`、`WnacgPanel.vue`、`AsmrPanel.vue` 等页面分散的手写粗糙分页，全部接入高质量的统一 `PaginationControl`。
- [ ] **8.2 标签管理 (`TagsView.vue`)**
  - 每个标签卡片增加“直达媒体库”跳转按钮（一键进入 `/type/xxx?tag=xxx`）；
  - 增加“一键清理 0 引用标签”按钮；
  - 合并标签弹窗增加搜索过滤，避免多标签时下拉难以寻找。
- [ ] **8.3 创作者视图 (`CreatorsView.vue`)**
  - 创作者列表顶部增加快速字母锚点或搜索过滤；
  - 创作者详情页返回按钮逻辑优化，网格作品补齐排序与类型筛选。
- [ ] **8.4 重复管理 (`DedupView.vue`)**
  - 双列比对的差异项增加直观的优劣色标（更高分辨率/更大码率高亮绿标）；
  - 点击缩略图可唤起全屏并排即时对比预览。

---

### 任务 9：偏好设置、外部工具与推荐 (`SettingsView`, `ExternalFavoritesView`, `MangaRecommendView`, `StatsView`)
- [ ] **9.1 外部收藏页面扁平化 (`ExternalFavoritesView.vue`)**
  - 将数据源折叠选择器改造为顶部平铺 Segmented Tabs，减少 1 次点击展开步骤。
- [ ] **9.2 偏好设置细节打磨 (`SettingsView.vue`)**
  - 主题色选择条紧凑化，腾出首屏给扫描文件夹；
  - 增加全站键盘快捷键帮助指南卡片；
  - 文件夹扫描过程增加更直观的动态进度/波纹动画与完成 Toast。
- [ ] **9.3 AI 漫画推荐抽屉与卡片排版 (`MangaRecommendView.vue`)**
  - 模型设置弹层增加点击外部自动收起（Click Outside）；
  - 推荐结果卡片网格统一垂直高度对齐，防止图文错位。
- [ ] **9.4 统计面板快速导航 (`StatsView.vue`)**
  - 顶部增加吸顶锚点导航条，点击快速平滑跳跃至【概览指标】【分布图表】【活跃趋势】【待整理项目】。
